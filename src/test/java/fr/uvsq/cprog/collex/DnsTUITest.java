package fr.uvsq.cprog.collex;
import fr.uvsq.cprog.collex.commande.*;

import static org.junit.Assert.*;

import org.junit.Test;

/**
 * Unit test for simple App.
 */
public class DnsTUITest {

    /* @Test
    public void testParseCommandeNom() {
        DnsTUI tui = new DnsTUI();
        Commande cmd = tui.nextCommande("www.uvsq.fr");
        assertNotNull(cmd);
    }

    @Test
    public void testParseCommandeAdd() {
        DnsTUI tui = new DnsTUI();
        Commande cmd = tui.nextCommande("add 193.51.31.90 www.uvsq.fr");
        assertNotNull(cmd);
    } */
}